// Create a function that adds two numbers (representing cupcakes on each tray) and returns the total.

function totalCupcakes(trayOne, trayTwo) {
  return trayOne + trayTwo;
}

console.log(totalCupcakes(3, 4));
